import React from 'react';
import PropTypes from 'prop-types';

Login.propTypes = {
    
};

function Login(props) {
    return (
        <div>
            This is login page
            <h1>login</h1>
        </div>
    );
}

export default Login;