import React from 'react';
import PropTypes from 'prop-types';

Admin.propTypes = {
    
};

function Admin(props) {
    return (
        <div>
            This is Admin page
        </div>
    );
}

export default Admin;