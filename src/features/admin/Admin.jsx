import React from 'react';
import PropTypes from 'prop-types';

Admin.propTypes = {
    
};

function Admin(props) {
    return (
        <div>
            This is Admin page
            <h1>Admin</h1>
        </div>
    );
}

export default Admin;